mixin video(video)
    div
        h4
            a(href=`/video/${video.id}`)=video.title
        p=video.description
        small=video.createdAt
        ul
            each hashtag in video.hashtags
                li=hashtag
        ul 
            li #{video.rating}/5
            li #{video.views} view
        hr